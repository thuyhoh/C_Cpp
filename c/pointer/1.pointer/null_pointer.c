#include <stdio.h>

int main(void)
{
    // NULL Pointer is a pointer that is pointing to nothing
    // NULL Pointer is a pointer that is pointing to nothing
    int *ptr = NULL;

    printf("The value of ptr is %p", ptr); // -> The value of ptr is (nil)
    return 0;
}
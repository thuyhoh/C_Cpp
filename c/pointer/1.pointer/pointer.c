#include <stdio.h>


void swap(int *a, int *b)
{
    int tmp = *a;
    *b = *a;
    *a = tmp;
}

int main(void){
/*  int a[10];
    double s[5];
    int *ptr1 = a;
    double *ptr2 = s;
 
    printf("sizeof(int)%d sizeof(double) %d\n",sizeof(int),sizeof(double));
    printf("address ptr1: %#X address ptr2: %#X \n",ptr1,ptr2);
    printf("address ptr1++: %#X address ptr2++: %#X \n",++ptr1,++ptr2);
    printf("address ptr1+2: %#X address ptr2+2: %#X \n",ptr1+2,ptr2+2);
    return 0;
*/
}